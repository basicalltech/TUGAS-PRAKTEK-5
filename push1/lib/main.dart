import 'package:flutter/material.dart';
import 'test_listview.dart'; // Mengimpor file test_listview.dart

void main() {
  runApp(const MaterialApp(
    home: ListViewTest(), // Menggunakan widget ListViewTest dari test_listview.dart sebagai halaman utama
  ));
}
